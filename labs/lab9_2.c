/*
 За потребите на професорите и асистентите на ФИНКИ потребно е да напишете програма која ќе врши скалирање на поените
 на студентите од еден испит.

Од стандарден влез потребно е да се вчитаат поени од определен испит на најмногу 100 студенти и истите да се зачуваат во низа.

Да се дефинира функција void scale која ќе прими како аргумент низа од броеви (поени на студентите на испитот).
 Функцијата потребно е да ги скалира поените на следниот начин:

Да се најдат максималните поени на испитот (пример 88 од 100)
Да се пресмета количникот 100/max (пример 100/88 = 1.13636)
Сите поени од низата да се помножат со овој количник со цел студентот којшто има најмногу поени на испитот да добие
 100 поени.
Да се испечати на екран низата со скалирани поени. Да не се користи помошна низа за решавање на задачата, односно
 трансформацијата на низата да биде  in-place.

For example:
--------------------------------------------------
 Input          |  Result
----------------|---------------------------------
 5              |
 10 20 30 40 82 |  12.20 24.39 36.59 48.78 100.00
--------------------------------------------------
 */


#include <stdio.h>
#define MAX 100

void scale(float *arr, int n);


int main(){

    float arr[MAX];
    int n;


    scanf("%d",&n);

    for(int i = 0; i < n; i++){
        scanf("%f", &arr[i]);
    }
    scale(arr, n);

    for(int i = 0; i < n; i++){
        printf("%.2f ", arr[i]);
    }

}

void scale(float *arr, int n){
    float maxVal = 0,max;

    for(int i = 0; i < n; i++){


        if(*(arr + i) > maxVal)
            maxVal = *(arr + i);

    }

    max = 100/maxVal;


    for(int i = 0; i < n; i++){
        *(arr + i) *= max;

    }
}